RewriteEngine On

# Allow access to real files (css, js, images)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send all other requests to index.php
RewriteRule ^ index.php [QSA,L]
